<%= turbo_frame_tag 'new_item_form' do %>
  <%= form_with(model: Item.new, data: { turbo_frame: 'new_item_form' }, class: "min-w-full") do |form| %>
    <%= form.text_field :body, data: { controller: "resetable" }, class: "min-w-full rounded-lg bg-slate-100 border-1 border-white focus:outline-none !" %>
  <% end %>
<% end %>
